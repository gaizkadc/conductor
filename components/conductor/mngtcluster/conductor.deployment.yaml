###
# Conductor configuration
###

kind: Deployment
apiVersion: apps/v1
metadata:
  labels:
    cluster: management
    component: conductor
  name: conductor
  namespace: nalej
spec:
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      cluster: management
      component: conductor
  template:
    metadata:
      labels:
        cluster: management
        component: conductor
    spec:
      containers:
      - name: conductor
        image: nalejregistry.azurecr.io/nalej/conductor:VERSION
        imagePullPolicy: Always
        securityContext:
          runAsUser: 2000
        args:
          - "run"
          - "--debug"
          - "--systemModelAddress=system-model.nalej:8800"
          - "--networkManagerAddress=network-manager.nalej:8000"
          #- "--appClusterPort=443"
        ports:
        - name: api-port
          containerPort: 5000
      imagePullSecrets:
      - name: nalej-registry
