kind: Deployment
apiVersion: apps/v1
metadata:
  labels:
    cluster: application
    component: musician
  name: musician
  namespace: __NPH_NAMESPACE
spec:
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      cluster: application
      component: musician
  template:
    metadata:
      labels:
        cluster: application
        component: musician
    spec:
      containers:
      - name: musician
        env:
          - name: CLUSTER_ID
            valueFrom:
              configMapKeyRef:
                name: cluster-config
                key: cluster_id
        image: __NPH_REGISTRY.azurecr.io/__NPH_REGISTRY_NAMESPACE/conductor:__NPH_VERSION
        imagePullPolicy: IfNotPresent
        securityContext:
          runAsUser: 2000
        args:
        - "musician"
        - "--prometheus=http://prometheus.__NPH_NAMESPACE.svc.cluster.local:9090"
        #- "--debug"
        ports:
        - name: musician-port
          containerPort: 5100
      imagePullSecrets:
      - name: nalej-registry
